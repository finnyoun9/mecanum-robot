# 真机闭环与 PID 调试路线

> 基线：2026-08-13，2026-08-22 更新真机开环状态。当前 ROS 2、协议、运动学和 SIL 已跑通；主控已确认为 **STM32F103C8T6**，接线方案见 [docs/wiring.md](wiring.md)，供电方案见 [docs/power-system.md](power-system.md)。ST-Link 已连接真机并跑通 PC13 bring-up、四路 PWM/方向/STBY 映射和四轮开环演示，验证记录与可复现构建命令见 [`firmware/Core/HW/README.md`](../firmware/Core/HW/README.md)。这不等于完整 FreeRTOS 机器人固件已真机验证：PID/编码器/协议还没有对应的真机外设初始化（MSP），真实底盘尚未形成编码器速度闭环。

## 项目边界

- `mecanum-robot` 是机器人主项目：树莓派 5 + ROS 2 + STM32 底盘 + 后续机械臂。
- `stm32-pid-balancer` 保留为控制算法实验台，不把“自平衡车”整套并入麦轮车。
- 可复用内容：通用 PID、抗积分饱和、UART 遥测、CSV 日志、阶跃响应分析和调参工具。
- 不直接复用内容：倒立摆直立环、平衡车机械模型和双轮输出混合。

复用前先修两处代码：`stm32-pid-balancer` back-calculation 必须在除以 `Kp` 前判断零值；`mecanum-robot` 的 D 项实现是误差差分，与“derivative on measurement”注释不一致。当前速度环先使用 PI（`Kd=0`），并为这两条边界增加 host test。

麦轮车需要的是四路电机速度 PI；自平衡车需要角度 PD + 速度 PI + 转向环，控制对象和安全风险都不同。

## M0：硬件基线

1. ~~记录 STM32 型号、电机型号、TB6612 引脚和电源结构~~ —— 已确认：STM32F103C8T6、TB6612 ×2（裸露 AIN1/AIN2/BIN1/BIN2 引脚）、MPU6050，引脚表见 [docs/wiring.md](wiring.md#已确认引脚分配stm32f103c8t62026-08-21)。电机型号/减速比/编码器线数/轮径待补。
2. ~~用万用表确认电机/逻辑电源、共地和短路；DP100 限流上电。~~ —— 已完成四轮 6 V 限流开环验证；正式电池供电改造按 [供电方案](power-system.md) 执行。
3. ~~补齐真机可编译固件~~ —— 项目没有 CubeMX .ioc，手动 vendor 了官方 STM32Cube HAL/CMSIS/FreeRTOS（`firmware/Core/Drivers`、`firmware/Core/Middleware`），链接脚本/启动文件复用 `remote_controller` 已验证过的路径。`firmware/Core/HW/` 下的最小 bring-up（时钟 HSI+PLL@64MHz + GPIO 翻转）已编译、烧录、经 GDB 读寄存器验证在真机上运行。**这只证明了工具链**。此后 `firmware/Core/HW/` 下的裸机目标已验证四路电机开环驱动和四路编码器软件解码（见该目录 README）。`encoder.c` 已于 2026-08-23 从走不通的硬件定时器模式移植为软件 EXTI 解码，并用 `encoder_port_check` 在真机上验证了生产代码本身。仍未接上的：`motor.c` 的引脚映射还没落到真实值（目前由各 HW target 在初始化时传入），以及 FreeRTOS 应用的外设 MSP 初始化（UART DMA、I2C），`firmware_arch_main()` 还不能在真机上跑。
4. 明确急停默认态、通信超时和上电禁止误转策略。
5. ~~完成真正的 UART DMA/IDLE~~ —— 已实现（DMA staging buffer 与软件 ring 分离，`HAL_UARTEx_RxEventCallback` 更新写指针，`HAL_UART_TxCpltCallback` 释放 TX 信号量，见 `firmware/Core/Src/main.c`）。这是 SIL 里验证过的逻辑，真机上还没跑过。

通过条件：~~引脚表、接线图和电源电流有记录；四个轮子可以被逐路、安全地开环驱动。~~ —— 已完成。下一阶段先稳定电源与线束，再接编码器。

## M1：单轮开环与编码器

1. ~~先只接一个电机测试 PWM~~ —— 已完成，先单独验证 FL，再四轮同测。
2. ~~确认编码器相位、方向~~ —— 已完成（2026-08-23，`encoder_debug`）：抬起底盘 40% 占空比跑 1.5 秒，四路计数 FL 631 / FR 633 / RL 640 / RR 619，全为正且整车物理前进，一致性约 3%。期间修正两处：`motor_set_tim()` 方向引脚按 (AIN2, AIN1) 传入使"正占空比 = 前进"；对调 FL 编码器 A/B 两线统一计数符号。计数倍频未做（当前只用上升沿单边沿触发）。
3. ~~测量各占空比下的稳态转速，得到死区和近似 `PWM -> rad/s` 曲线~~ —— 已完成，见下方"占空比 → 转速实测曲线"。
4. 连续记录时间戳、目标速度、实测速度、PWM 和编码器增量。

换算基准已实测（2026-08-23）：手动转右前轮 10 圈两次，计数 2240 / 2244，即 **224 edges/圈**（`firmware/Core/HW/encoder_count_main.c`，两次相差 0.18%）。轮径 60 mm 实物确认，半径 0.030 m。

原先 `encoder.h` 的推导值 `11 × 4 × 34 = 1496` 错了 6.7 倍，两个原因叠加：减速比实际约 1:20 不是 1:34（224 ÷ 11 ≈ 20.4），且软件 EXTI 解码只数上升沿（1 倍频），不是硬件正交的 4 倍频。里程计直接按这个数缩放，用错等于距离读数差同样倍数。已同步修正三处：`encoder.h`、ROS 2 硬件接口 `mcr_hardware_interface.cpp`、协议模拟器 `tools/stm32_uart_sim.py`；轮径同步修正 URDF 和 `controllers.yaml`（原按 65 mm）。

### 占空比 → 转速实测曲线（2026-08-23）

抬起底盘，`firmware/Core/HW/duty_sweep_main.c` 逐级驱动四轮，每级稳定 300 ms 后测 1 秒窗口内的编码增量。下表为四轮平均，按 224 edges/圈换算：

| 占空比 | edges/s | rev/s | rad/s | m/s |
|---|---|---|---|---|
| 5% | **0** | 0 | 0 | 0 |
| 10% | 71.5 | 0.32 | 2.01 | 0.060 |
| 15% | 120 | 0.54 | 3.37 | 0.101 |
| 20% | 169 | 0.75 | 4.73 | 0.142 |
| 30% | 267 | 1.19 | 7.49 | 0.225 |
| 40% | 366 | 1.63 | 10.25 | 0.308 |
| 50% | 464 | 2.07 | 13.02 | 0.391 |
| 60% | 563 | 2.51 | 15.79 | 0.474 |
| 70% | 662 | 2.96 | 18.57 | 0.557 |
| 80% | 760 | 3.39 | 21.32 | 0.640 |

**线性度**：20% 以上斜率稳定在约 **0.986 edges/(duty·s)**，每加 10% 占空比增加约 99 edges/s，接近理想直线——被控对象线性，PID 不需要增益调度。

**死区**：5% 时四轮完全不转，10% 正常。线性段向下外推交于占空比约 **3%**，但实测 5% 起不来——差值来自静摩擦，**启动死区（5%~10% 之间）高于线性外推死区**，PID 前馈/抗积分饱和要按启动死区处理。死区具体位置未做更细分辨率的测量。

**四轮一致性**：80% 时 RL 最快 771、RR 最慢 750，差 2.8%；各轮斜率 0.973~1.000。差异小，四路 PID 可先用同一组参数起步。

**外推满占空比**：约 4.27 rev/s ≈ **0.81 m/s** 车速上限（SIL 的 `max_wheel_rps` 已按此更新）。

⚠️ **该曲线绑定当时的供电电压**（DP100 台面供电，非电池）。转速与供电电压近似成正比，换成 3S 电池（11.1–12.6 V，且随电量下降）后曲线会整体平移，上车后需复测。 —— **已于 2026-08-25 复测，见下节。**

### 占空比 → 转速实测曲线（3S 电池供电，2026-08-25）

**实测条件**：电池电压 **11.47 V**（万用表实测，3S 约 3.82 V/节，中等电量）、底盘抬起空载、TB6612 `VM` 直接接电池总线、MCU 由 ST-Link 3.3 V 供电。每档稳定 300 ms 后测 1 秒窗口，2 倍频解码（448 edges/圈）。

| 占空比 | FL | FR | RL | RR | 平均 | rev/s | rad/s | m/s |
|---|---|---|---|---|---|---|---|---|
| 5% | 0 | 0 | 0 | 0 | **0** | 0 | 0 | 0 |
| 10% | 151 | 152 | 152 | 148 | 150.8 | 0.34 | 2.11 | 0.063 |
| 15% | 253 | 253 | 254 | 248 | 252.0 | 0.56 | 3.53 | 0.106 |
| 20% | 355 | 354 | 358 | 347 | 353.5 | 0.79 | 4.96 | 0.149 |
| 30% | 562 | 558 | 566 | 534 | 555.0 | 1.24 | 7.78 | 0.234 |
| 40% | 769 | 762 | 775 | 720 | 756.5 | 1.69 | 10.61 | 0.318 |
| 50% | 976 | 967 | 986 | 925 | 963.5 | 2.15 | 13.51 | 0.405 |
| 60% | 1183 | 1171 | 1195 | 1136 | 1171.3 | 2.61 | 16.43 | 0.493 |
| 70% | 1391 | 1373 | 1406 | 1334 | 1376.0 | 3.07 | 19.30 | 0.579 |
| 80% | 1598 | 1576 | 1617 | 1542 | 1583.3 | 3.53 | 22.21 | 0.666 |

**与 DP100 台面供电对比：整体快 3.3%~5.4%，平均约 +4.2%**（推导值，逐档比值）。

> **结论：M2 的 `Kp=100 / Ki=300` 不需要因为换电池重调。** 4% 的对象增益变化对带积分项的 PI 可以直接吸收。落地带载后仍需复调——那是另一回事，改变的是负载不是电压。

**死区不变**：5% 四轮全部为 0，10% 正常起转，与台面供电时一致。

**线性度保持**：20% 以上斜率 **20.5 edges/(duty·s)**（2 倍频口径）。**外推**满占空比约 **4.45 rev/s ≈ 0.84 m/s**（原台面供电外推 4.27 rev/s / 0.81 m/s）。注意这是外推值，未实测。

⚠️ **四轮一致性 4.9%**（RL 最快 1617，RR 最慢 1542），比台面供电时的 2.8% 略宽，**RR 明显偏慢**。M3 四轮闭环**不要默认四路共用一组 PID 参数**。

> 📌 关于 DP100 当时的电压：原始记录**没有写**，这是个记录缺口。从两次曲线只差 4% 反推，当时应在 **11 V 附近**（不可能是 6 V，那样会差一倍）。这是推断，不是实测。

**仍未实测**：带载（落地）转速、堵转电流、阶跃响应。

通过条件：正向 PWM 对应正向转速；重复三次的稳态转速偏差可解释；堵转能立即停机。

## M2：单轮速度 PI —— 已完成（2026-08-23，右前轮 FR，空载，DP100 供电）

工具：`firmware/Core/HW/pid_step_main.c` + `pid_run.sh` + `pid_metrics.py`。增益放在 RAM 由 GDB 写入，整轮调参只烧录一次。

### 前置修正：编码器改 2 倍频

1 倍频时 100 Hz 采样的速度分辨率是 **2.805 rad/s**（1 个计数 / 10 ms = 100 edges/s ÷ 224 × 2π），目标 10 rad/s 只有 3.5 个刻度，实测速度在两档之间反复跳变——**在这种信号上调 PID 没有意义**。改为 A 相双边沿（2 倍频）后分辨率降到 1.40 rad/s，`EDGES_PER_WHEEL_REV` 相应 224 → 448。

真机验证：同一占空比下计数 1135/1131/1151/1111，正好是 1 倍频时 577/577/582/565 的 **1.96–1.98 倍**。没有超过 2 倍，说明没有把噪声毛刺计进去——这也回答了"要不要加滤波电容"：**不需要**。

未做 4 倍频：需要 B 相也接中断，但 EXTI 线按引脚编号全芯片复用，FR 的 PA7 与 RL 的 PB7 争用线路 7（PA6/PB6 同理），**必须重新接线**才能实现。

### Kp 扫描（Ki=0）

| Kp | 稳态误差 | 超调 | IAE | 输出饱和 |
|---|---|---|---|---|
| 40 | +51.0% | 无 | 11.84 | 0/230 |
| **100** | +28.5% | +12.2% | **6.63** | 1/230 |
| 200 | +20.5% | +40.2% | 7.91 | 56/230 |

取 Kp=100：IAE 最小；Kp=200 超调翻倍且四分之一时间输出饱和，已过激进。实测值与按占空比曲线推算的理论值吻合（预测 4.68 / 6.98 / 8.24，实测 4.90 / 7.15 / 7.95），说明被控对象模型可信。

### Ki 扫描（Kp=100）

| Ki | 稳态误差 | 超调 | IAE |
|---|---|---|---|
| 200 | +1.6% | +12.2% | 2.24 |
| **300** | **+0.2%** | **+12.2%** | **2.03** |
| 500 | +0.2% | +26.2% | 1.95 |
| 1000 | −0.2% | +26.2% | 1.93 |

**最终增益：Kp = 100，Ki = 300，Kd = 0。**

同时修正了抗积分饱和：`pid.c` 钳位的是积分**本身**而非积分项（项 = Ki × 积分），原先固定 `integral_max=1000` 在 Ki=500 时允许积分项达到 500000，是输出上限的 500 倍，等于没有抗饱和。现按 `out_max / Ki` 计算，使积分单独贡献恰好封顶于满输出。

### 三档速度验证（Kp=100, Ki=300）

| 目标 | 稳态误差 | 超调 | IAE | 循环周期 |
|---|---|---|---|---|
| 5 rad/s | +0.4% | +40.2% | 2.12 | 10000 µs |
| 10 rad/s | +0.2% | +12.2% | 2.15 | 10000 µs（9997–10002）|
| 15 rad/s | +0.4% | +12.2% | 2.74 | 10000 µs |

**控制周期抖动 ±3 µs**，100 Hz 环稳定，不存在错过截止时间的问题。

⚠️ **超调数字受测量分辨率支配，不要当作真实性能差异**：三档的绝对超调分别是 2.0 / 1.22 / 1.83 rad/s，都约等于 1 个量化台阶（1.40 rad/s）。低速时百分比被放大只因分母小。当前测量手段分辨不出 14% 以下的超调差异。

⚠️ **空载、台面供电下的结果**。落地带载和换 3S 电池后都需要复调——负载改变被控对象增益，供电电压改变占空比→转速关系。

> ✅ **2026-08-25 更新：电池供电这一半已经验证过了。** 11.47 V 电池下的占空比曲线只比台面供电快约 4%（见 M1 的电池曲线一节），这点增益变化 PI 的积分项可以直接吸收，**`Kp=100 / Ki=300` 不需要因为换电池重调**。仍未验证的是**落地带载**——那改变的是负载，不是供电电压，属于 M3 的范围。

通过条件：~~三档速度均稳定；参数有指标可比较~~ —— 已达成。

## M3：四轮闭环

**当前实现（2026-08-25，尚未真机）**：新增独立裸机目标
`firmware/Core/HW/remote_pid_drive_main.c`，不替换已验收的开环 `remote_drive`。
它复用生产 `encoder.c`、`pid.c`、NRF24 接收和遥控运动学，在 100 Hz 对四轮各跑一个
PI（初值 `Kp=100, Ki=300, Kd=0`），并将每轮目标限为 20 rad/s。启动默认禁用；K1 才使能；
K9、250 ms 遥控超时、超速或持续高 PWM 无编码器反馈都会关闭两块 TB6612 的 STBY。该目标已
在 GNU 工具链以 `-Wall -Werror` 编译；未烧录、未台面或落地验证，因此下列 M3 条目仍全部待完成。

1. 四个电机分别测死区和增益，不默认四路参数完全一致。**已有实测依据**：2026-08-25 电池供电扫描下四轮一致性 4.9%（RL 最快、**RR 最慢**），比台面供电时的 2.8% 更宽，RR 需要单独确认增益。
2. 测试前进、横移、原地旋转时四轮目标符号和实际方向。
3. 空载后再落地测试，观察电池电压、轮胎打滑和机械装配误差。
4. 加入通信失联、编码器无跳变和超速保护。

通过条件：四轮闭环持续 30 min 无失控；三种基本运动均可重复；急停和 250 ms 遥控通信超时有效（手柄标称 10 Hz，须容忍调度抖动与单帧丢失）。

## M4：Pi 5 + STM32 HIL

> ⚠️ `lx = 0.10 m` 和 `ly = 0.12 m` 仍是**未实测的估计值**。在真车上用尺量前后轮
> 轴中心距得到轴距、量左右轮轴中心距得到轮距，分别除以 2 后替换默认值；它们会直接影响
> 横移和旋转里程计，不能因为前进方向看起来正常就当作已验证。

1. Pi 5 发送 `/cmd_vel`，通过共享协议下发四轮目标速度。
2. STM32 回传编码器/轮速；ROS 2 硬件接口发布真实 joint state 和 odometry。
3. 对比协议模拟器与真机日志；用实测 `lx/ly` 替换默认估计值，并修正轮径、编码器比例和方向。
4. 录制前进 1 m、横移 1 m、原地 360° 三项测试，报告位置/角度误差。

通过条件：链路从 `/cmd_vel` 到真实 PWM、再从编码器回到 `/odom` 闭合；用实测 `lx/ly`
替换默认值；不再把协议模拟器数据写成真机结果。

## M5：再加传感器与机械臂

- 底盘稳定后再接 IMU、ToF、NRF24L01 和 Nav2。
- 机械臂当前只保留 host protocol 与 SIL 骨架；收到硬件后按单舵机、六轴、ROS 2、上车联动推进。
- 上板前清除 `arm_controller` 的 packed member 未对齐编译警告，并以 `-Wall -Wextra -Werror` 构建。
- 机械臂未完成舵机总线和实物联动前，不单独作为简历主项目。

## PID 难度判断

| 内容 | 预计难度 | 建议投入 |
|---|---|---|
| 单电机速度 PI | 中等，硬件正常后 1–2 天可见结果 | 当前优先 |
| 四轮速度闭环 | 中等偏高，主要难在一致性、电源和方向 | 当前主线 |
| 位置环 | 中等，可作为控制实验 | 非求职必需 |
| 自平衡串级 PID | 高，机械重心、IMU 零偏、周期抖动和电源都会影响 | 求职后或底盘完成后再做 |
